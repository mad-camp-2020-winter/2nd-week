<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rentalservice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentalService">
        <activity android:name=".activity.JoinActivity"></activity>
        <activity android:name=".activity.UserRentalSelectActivity" />
        <activity android:name=".activity.UserInfoDetailActivity" />
        <activity android:name=".activity.LoginActivity">

        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".activity.InfoEditActivity" />
        <activity android:name=".activity.GalleryAddActivity" />
        <activity android:name=".activity.GalleryEditActivity" />
        <activity
            android:name=".activity.AdminCommentActivity"
            android:theme="@android:style/Theme.Dialog"/>

        <activity
            android:name=".activity.InfoAddActivity"
            android:theme="@android:style/Theme.Dialog" />
        <activity android:name=".activity.InfoDetailActivity" />
        <activity android:name=".activity.SelectLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.UserMainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RentalService.NoActionBar"
            />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RentalService.NoActionBar">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
    </application>

</manifest>